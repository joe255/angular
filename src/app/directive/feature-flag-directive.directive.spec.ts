import { FeatureFlagDirectiveDirective } from './feature-flag-directive.directive';

describe('FeatureFlagDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureFlagDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
